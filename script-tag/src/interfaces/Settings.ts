export interface Settings {
    // Emoji Options
    firstEmoji: string;
    secondEmoji: string;

    // Style Options
    backgroundColor: string;
    selectionColor: string;
    strokeColor: string;
    strokeWidth: number;
    cornerRadius: number;

    // Text Options
    labelText: string;
    tooltipText: string;

    // Visibility
    displayStatus: boolean;

    // Price Options
    firstPrice: number;
    secondPrice: number;

}