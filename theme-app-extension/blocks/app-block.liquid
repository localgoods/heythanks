{% if cart.item_count > 0 %}
  <tips-block id="tips" data-id="{{ block.id }}" class="page-width">
    <div class="tips-wrapper">
      <article>
        <span class="tips__header">Send some love to your hardworking fulfillment workers alongside your order 💜</span>
        <span class="tips__subheader no-wrap">
          Powered by <img src="{{ 'HeyThanks.svg' | asset_url }}" loading="lazy" height="calc(1.8 * 14px)" width="auto" class="logo-img" /> <img src="{{ 'QuestionMarkInverseMajor.svg' | asset_url }}" loading="lazy" height="auto" width="14" class="help-icon" />
        </span>
      </article>

      <input type="radio" id="radio-1" name="tip-options">
      <label for="radio-1" class="animated unselectable">
        <div class="label-inner">
          <span class="tips__radio-control animated"></span>
          {%- if block.settings.emoji_1 != 'None' -%}
            <span class="tips__radio-emoji">{{ block.settings.emoji_1 }}</span>
          {%- endif -%}
          {%- if all_products["fulfillment-tip"].variants[0] -%}
            <span class="tips__radio-price">{{ all_products["fulfillment-tip"].variants[0].price | money }}</span>
          {%- endif -%}
        </div>
      </label>

      <input type="radio" id="radio-2" name="tip-options">
      <label for="radio-2" class="animated unselectable">
        <div class="label-inner">
          <span class="tips__radio-control animated"></span>
          {%- if block.settings.emoji_2 != 'None' -%} 
            <span class="tips__radio-emoji">{{ block.settings.emoji_2 }}</span>
          {%- endif -%}
          {%- if all_products["fulfillment-tip"].variants[1] -%}
            <span class="tips__radio-price">{{ all_products["fulfillment-tip"].variants[1].price | money }}</span>
          {%- endif -%}
        </div>
      </label>
    </div>
    {% if request.design_mode %}
      <div id="overlay"></div>
      <div id="recommendation">
        <b>👋 Let's position HeyThanks</b>
        <p>Hover over the HeyThanks "Apps" section in your left sidebar and hold down the <img src="{{ 'DragHandleMinor.svg' | asset_url }}" loading="lazy" height="calc(1.8 * 1.6rem)" width="calc(1.8 * 1.6rem)" class="icon-img" /> icon to start dragging.</p>
        <p>For most themes, we recommend that you position the HeyThanks <b>Apps</b> section between the <b>Items</b> and <b>Subtotal</b> sections.</p>
      </div>
      <span id="close" onclick="hideOverlay()"></span>
    {% endif %}
  </tips-block>

{% endif %}
  
{% if request.design_mode %}
  <section id="design" class="page-width">
    <div class="design-wrapper">
      <p>
        <i>We see you're in design mode...</i><br>
        Add a product to your cart to edit your <b>HeyThanks</b> tipping options!
      </p>
    </div>
  </section>
{% endif %}

{% schema %}
  {
    "name": "Tips block",
    "target": "section",
    "stylesheet": "tips-block.css",
    "javascript": "tips-block.js",
    "templates": ["cart"],
    "settings": [
      {
        "type": "header",
        "content": "Fulfillment Tip - Option 1"
      },
      {
        "type": "select",
        "id": "emoji_1",
        "label": "Emoji",
        "options": [
          {
            "value": "None",
            "label": "None"
          },
          {
            "value": "🙂",
            "label": "🙂"
          },
          {
            "value": "🏎",
            "label": "🏎"
          }
        ],
        "default": "🙂"
      },
      {
        "type": "header",
        "content": "Fulfillment Tip - Option 2"
      },
      {
        "type": "select",
        "id": "emoji_2",
        "label": "Emoji",
        "options": [
          {
            "value": "None",
            "label": "None"
          },
          {
            "value": "🥰",
            "label": "🥰"
          },
          {
            "value": "🚀",
            "label": "🚀"
          }
        ],
        "default": "🥰"
      }
    ]
  }
{% endschema %}
